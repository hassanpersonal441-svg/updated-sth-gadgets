'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import type {Category} from '@/types/database';
export default function SeriesCreateForm({categories}:{categories:Category[]}){const [name,setName]=useState('');const [description,setDescription]=useState('');const [brand,setBrand]=useState('');const [error,setError]=useState('');const router=useRouter();async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/series',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,description,brand:brand||null,category_id:(e.currentTarget as HTMLFormElement).category_id?.value||null})});const d=await r.json();if(!r.ok){setError(d.error||'Unable to create series');return}router.push(`/admin/series/${d.series.id}`);router.refresh()}return <form onSubmit={save} className="max-w-2xl space-y-4 rounded-2xl border border-slate-800 bg-[#0C1420] p-6"><label className="block text-sm text-silver-dim">Series name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg bg-[#080D15] p-3 text-silver-bright"/></label><label className="block text-sm text-silver-dim">Brand<input value={brand} onChange={e=>setBrand(e.target.value)} className="mt-1 w-full rounded-lg bg-[#080D15] p-3 text-silver-bright"/></label><label className="block text-sm text-silver-dim">Category<select name="category_id" className="mt-1 w-full rounded-lg bg-[#080D15] p-3 text-silver-bright"><option value="">No category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="block text-sm text-silver-dim">Description<textarea value={description} onChange={e=>setDescription(e.target.value)} className="mt-1 w-full rounded-lg bg-[#080D15] p-3 text-silver-bright"/></label>{error&&<p className="text-red-400">{error}</p>}<button className="rounded-lg bg-[#00C4CC] px-5 py-3 font-bold text-black">Create Series</button></form>}
